def countBits(n):
    return n.bit_length()
    #x = '{0:b}'.format(n)
    #b = "1"
    #c = "0"
    #str(x)
    #a = str(x).count(b) + str(x).count(c)
    #return a
    
def modulus(n):
    if (n-int(n)) == 0 :
        return n % 2
    else:
        return -1

